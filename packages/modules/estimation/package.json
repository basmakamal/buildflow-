{
  "name": "@buildflow/estimation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./pricing": "./src/pricing.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@buildflow/core": "workspace:*",
    "@buildflow/database": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^3.2.4"
  },
  "//exports": "Two entry points. The ROOT carries the Prisma repositories and is server only; ./pricing is pure arithmetic the 3D viewer imports to price a finish change live."
}
