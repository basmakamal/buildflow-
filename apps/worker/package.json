{
  "name": "@buildflow/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts",
    "once": "tsx src/main.ts --once"
  },
  "dependencies": {
    "@buildflow/catalogue": "workspace:*",
    "@buildflow/core": "workspace:*",
    "@buildflow/database": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@prisma/client": "^5.22.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^3.2.4"
  }
}
